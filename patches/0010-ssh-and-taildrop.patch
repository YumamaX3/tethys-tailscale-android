0010 - SSH & Taildrop
=====================

The two features the stock Android app never exposed: Tailscale SSH and Taildrop, with Android-safe file operations.

concern   : ssh-and-taildrop
files     : 9
upstream  : squashed android patch d6a7411 (tailscale v1.98.5 -> v1.98.8), 2026-07-13
apply     : git apply patches/0010-ssh-and-taildrop.patch

Regrouped by concern from the upstream squashed patch - the ten commits named in
that squash are not recoverable from it, so this is a reviewed regrouping, not a
recovery. The split is lossless and machine-checked; see patches/MANIFEST.md.
Everything above the first "diff --git" is a preamble and is ignored by git apply.

diff --git a/feature/ssh/ssh.go b/feature/ssh/ssh.go
index bd22005916d60..647181115f1ab 100644
--- a/feature/ssh/ssh.go
+++ b/feature/ssh/ssh.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build ((linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9) && !ts_omit_ssh
+//go:build (linux || (darwin && !ios) || freebsd || openbsd || plan9) && !ts_omit_ssh
 
 // Package ssh registers the Tailscale SSH feature, including host key
 // management and the SSH server.
diff --git a/feature/taildrop/fileops_fs.go b/feature/taildrop/fileops_fs.go
index 3ddf95d0314cd..070d12ccb38e5 100644
--- a/feature/taildrop/fileops_fs.go
+++ b/feature/taildrop/fileops_fs.go
@@ -1,6 +1,5 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
-//go:build !android
 
 package taildrop
 
diff --git a/feature/taildrop/paths.go b/feature/taildrop/paths.go
index 76054ef4d58b3..d74cda0a52d26 100644
--- a/feature/taildrop/paths.go
+++ b/feature/taildrop/paths.go
@@ -7,6 +7,7 @@ import (
 	"fmt"
 	"os"
 	"path/filepath"
+	"runtime"
 
 	"tailscale.com/version/distro"
 )
@@ -25,6 +26,14 @@ func (e *Extension) SetFileOps(fileOps FileOps) {
 }
 
 func (e *Extension) setPlatformDefaultDirectFileRoot() {
+	if runtime.GOOS == "android" {
+		dir := "/sdcard/Download/Taildrop"
+		if err := os.MkdirAll(dir, 0o700); err == nil {
+			e.directFileRoot = dir
+		}
+		return
+	}
+
 	dg := distro.Get()
 
 	switch dg {
diff --git a/ssh/tailssh/c2n.go b/ssh/tailssh/c2n.go
index 621be74d4baba..821e4c23b6c2c 100644
--- a/ssh/tailssh/c2n.go
+++ b/ssh/tailssh/c2n.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build linux || (darwin && !ios) || freebsd || openbsd || plan9
 
 package tailssh
 
diff --git a/ssh/tailssh/hostkeys.go b/ssh/tailssh/hostkeys.go
index 8046a021a9308..358e6dd4f9033 100644
--- a/ssh/tailssh/hostkeys.go
+++ b/ssh/tailssh/hostkeys.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build linux || (darwin && !ios) || freebsd || openbsd || plan9
 
 package tailssh
 
diff --git a/ssh/tailssh/incubator.go b/ssh/tailssh/incubator.go
index 48c65e8e51446..6d3ccc57b3ff6 100644
--- a/ssh/tailssh/incubator.go
+++ b/ssh/tailssh/incubator.go
@@ -7,7 +7,7 @@
 // and groups to the specified `--uid`, `--gid` and `--groups`, and
 // then launches the requested `--cmd`.
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd
+//go:build (linux && !android) || android || (darwin && !ios) || freebsd || openbsd
 
 package tailssh
 
@@ -48,6 +48,7 @@ import (
 
 const (
 	linux   = "linux"
+	android = "android"
 	darwin  = "darwin"
 	freebsd = "freebsd"
 	openbsd = "openbsd"
@@ -846,6 +847,16 @@ func (ss *sshSession) launchProcess() error {
 
 	cmd := ss.cmd
 	cmd.Env = envForUser(ss.conn.localUser)
+	if runtime.GOOS == "android" {
+		if home, exists := os.LookupEnv("HOME"); exists {
+			cmd.Dir = home
+		}
+		for _, kv := range os.Environ() {
+			if !strings.HasPrefix(kv, "TS_") {
+				cmd.Env = append(cmd.Env, kv)
+			}
+		}
+	}
 	for _, kv := range ss.Environ() {
 		if acceptEnvPair(kv) {
 			cmd.Env = append(cmd.Env, kv)
@@ -1167,6 +1178,8 @@ func (ia *incubatorArgs) loginArgs(loginCmdPath string) []string {
 			return []string{loginCmdPath, "-f", ia.localUser, "-p"}
 		}
 		return []string{loginCmdPath, "-f", ia.localUser, "-h", ia.remoteIP, "-p"}
+	case android:
+		return []string{loginCmdPath}
 	case freebsd, openbsd:
 		return []string{loginCmdPath, "-fp", "-h", ia.remoteIP, ia.localUser}
 	}
@@ -1196,6 +1209,16 @@ func setGroups(groupIDs []int) error {
 		groupIDs = groupIDs[:16]
 	}
 
+	if runtime.GOOS == "android" {
+		if os.Geteuid() == 0 {
+			err := syscall.Setgroups(groupIDs)
+			if err != nil {
+				fmt.Println("Setgroups failed:", err)
+			}
+		}
+		return nil
+	}
+
 	err := syscall.Setgroups(groupIDs)
 	if err != nil && os.Geteuid() != 0 && groupsMatchCurrent(groupIDs) {
 		// If we're not root, ignore a Setgroups failure if all groups are the same.
diff --git a/ssh/tailssh/incubator_linux.go b/ssh/tailssh/incubator_linux.go
index cff46160758f3..3539ffc0a2a03 100644
--- a/ssh/tailssh/incubator_linux.go
+++ b/ssh/tailssh/incubator_linux.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build linux && !android
+//go:build linux
 
 package tailssh
 
diff --git a/ssh/tailssh/tailssh.go b/ssh/tailssh/tailssh.go
index e01f78eb3ae50..a7ef570e740ae 100644
--- a/ssh/tailssh/tailssh.go
+++ b/ssh/tailssh/tailssh.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build (linux && !android) || android || (darwin && !ios) || freebsd || openbsd || plan9
 
 // Package tailssh is an SSH server integrated into Tailscale.
 package tailssh
diff --git a/ssh/tailssh/user.go b/ssh/tailssh/user.go
index 0d2bf31e7f0d5..0b9db03d704f9 100644
--- a/ssh/tailssh/user.go
+++ b/ssh/tailssh/user.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build (linux && !android) || android || (darwin && !ios) || freebsd || openbsd || plan9
 
 package tailssh
 
